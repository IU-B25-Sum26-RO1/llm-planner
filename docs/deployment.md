# Установка и запуск

Полная инструкция: [README.md](../README.md).

Кратко:

```bash
cp .env.example .env
# скачайте Vosk-модель в ./models
# настройте LLM_API_URL / LLM_MODEL в .env

docker compose build
docker compose up
```
